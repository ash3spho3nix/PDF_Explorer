# Storage Provider Framework

Provides a unified interface to access PDF documents from different storage backends.

## Supported Providers
- **LocalFilesystemProvider** – any local folder
- **GoogleDriveProvider** – Google Drive (My Drive, Shared Drives, Shared with me)

## Installation
```bash
pip install -e .